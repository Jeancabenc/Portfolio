Jean Carlos Bencosme · Portfolio Suite

This folder contains:
- portfolio-site/ (main portfolio)
- fights-hub/ (boxing events app)
- autocustomz/ (multi-page automotive site)
- fivem-atm-system/ (ATM UI demo)
- stats-dashboard/ (Chart.js dashboard)
- mytech-tools/ (JS utility collection)

For fetch() and Chart.js to work correctly, run a simple static server in this folder
(e.g. VS Code Live Server, `python -m http.server`, or `npx serve`).
